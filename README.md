# sphae.re

Personal static site for sphae.re. Plain HTML/CSS/JS, no build step.

Served via Caddy from this directory — see `../../Caddyfile` and `../../docker-compose.yml` in the parent `n8n` deployment repo.
